#include "bits/stdc++.h"
using namespace std;

#define chk(x) cout << (#x) << " is " << (x) << endl;
#define chk2(x, y) cout << (x) << " " << (y) << endl;
#define chkp(x) chk2(x.first, x.second);
#define chk3(x, y, z) cout << (x) << " " << (y) << " " << (z) << endl;
#define nl cout << '\n';
#define ll long long

auto solve(string A) {
}


int main() {
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    auto lol = solve();
    cout << endl << endl << lol << endl;
    // for (auto lmao : lol) cout << lmao << "\n";
    // for (auto lmao : lol) {
    //     for (auto rofl : lmao)
    //         cout << rofl << endl;
    //     cout << endl;
    // }
    return 0;
}